"use client";

/**
 * La taille des textes est désormais réglée explicitement dans l’onglet
 * Objets (liste de tailles + saisie numérique). La poignée cartographique est
 * conservée comme composant vide pour ne pas casser les imports existants.
 */
export function SelectedTextResizeHandle() {
  return null;
}
